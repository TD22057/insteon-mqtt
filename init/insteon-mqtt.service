[Unit]
Description=Insteon MQTT Bridge
After=network.target

[Service]
WorkingDirectory=/opt/insteon-mqtt
Type=simple
User=insteon
ExecStart=./bin/insteon-mqtt start config.yaml

[Install]
WantedBy=multi-user.target
